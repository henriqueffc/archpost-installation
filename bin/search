#!/usr/bin/env bash

pacman -Slq | fzf --preview "pacman -Si {} && echo 'Requerido por:' && expac -S '%N' {} && echo '' && if pacman -Q {} 2>/dev/null; then echo '✓ INSTALADO'; else echo '✗ NÃO INSTALADO'; fi" --keep-right --preview-window='nohidden:wrap',up,60% --height 90%
